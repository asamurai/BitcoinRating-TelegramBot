module.exports = {
    
}
/*
    Commands: 
        1. #usd #today / #usd / #today /getTodayRaiting(currency)
        2. #usd #yesterday / #yesterday /getYesterdayRaiting(currency)
        3. #usd #start=YYYY-MM-DD #end=YYYY-MM-DD / #start=YYYY-MM-DD #end=YYYY-MM-DD /getPeriodRaiting(currency, start, end)
        4. #start
        5. #commands
 */